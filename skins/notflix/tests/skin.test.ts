import { describe, expect, it } from 'vite-plus/test';

import { NotflixSkin } from '../src/react/index';

// Placeholder: the port replaces this file with microvideo's parity guards (skins/microvideo/tests/skin.test.ts).
describe('NotflixSkin', () => {
  it('exists until the port lands', () => {
    expect(NotflixSkin).toBeTypeOf('function');
  });
});
