version: 2
updates:
  - package-ecosystem: 'npm'
    directories:
      - '/'
    schedule:
      interval: 'daily'
    groups:
      prod-dependencies:
        patterns:
          - 'media-chrome'
    allow:
      - dependency-name: 'media-chrome'
