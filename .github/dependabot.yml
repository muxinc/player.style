version: 2
updates:
  - package-ecosystem: 'npm'
    directories:
      - '/'
    schedule:
      interval: 'daily'
    commit_message:
      prefix: 'fix'
      prefix_development: 'chore'
    groups:
      prod-dependencies:
        patterns:
          - 'media-chrome'
    allow:
      - dependency-name: 'media-chrome'
